# B端管理员登录接口改动说明

日期：2026-08-03

## 概述

将原来 B/C 端共用的登录接口拆分，新增 B 端专属管理员登录接口。USER 角色用户无法登录 B 端。

## 接口变更

### 新增接口

**B端管理员登录**

```
POST /api/admin/auth/login
Content-Type: application/json

请求体：
{
    "phone": "string",    // 手机号，必填
    "password": "string"  // 密码，必填
}

成功响应：
{
    "code": 1,
    "msg": "成功",
    "data": {
        "token": "string",                    // 会话令牌，后续请求放 Authorization 头
        "user": {
            "id": 1,
            "phone": "138xxxx",
            "email": "xxx@qq.com",
            "role": 1                         // 1=管理员
        }
    }
}

失败响应（非管理员账号）：
{
    "code": 1009,
    "msg": "该账号无管理员权限"
}
```

### 原有接口行为变更

**C端用户登录（不变更路径，但仅用于 C 端）**

```
POST /api/auth/login
```

原有逻辑不变，但不应用于 B 端登录。C 端 USER 和 ADMIN 均可登录成功。

## 新增错误码

| 错误码 | 说明 |
|--------|------|
| 1009 | 该账号无管理员权限 |

## 前端适配要点

1. **B 端登录页**：将登录接口从 `POST /api/auth/login` 改为 `POST /api/admin/auth/login`
2. **Token 使用方式不变**：响应中的 `token` 字段放在请求头 `Authorization: Bearer {token}` 中使用
3. **错误处理**：新增 1009 错误码，提示用户"该账号无管理员权限"
4. **C 端登录不变**：C 端继续使用 `POST /api/auth/login`

---

# DTO 参数格式校验

日期：2026-08-03

## 概述

给登录、注册、找回密码接口的 DTO 增加了手机号和邮箱的格式校验，后端会在参数校验阶段直接拦截非法格式。

## 校验规则

| DTO | 字段 | 新增注解 | 规则 |
|-----|------|----------|------|
| LoginDTO | phone | `@Pattern` | `^1[3-9]\d{9}$` 11位手机号 |
| RegisterDTO | phone | `@Pattern` | `^1[3-9]\d{9}$` 11位手机号 |
| RegisterDTO | email | `@Email` | 标准邮箱格式 |
| ResetPasswordDTO | email | `@Email` | 标准邮箱格式 |

## 前端适配要点

- 前端已有格式校验的不受影响，加上后是双重保障
- 不合法格式会返回 `code: 400`，`msg` 为具体的校验消息（如"手机号格式不正确"、"邮箱格式不正确"）

---

# C端邮箱验证码登录

日期：2026-08-03

## 概述

C 端新增邮箱验证码登录方式，原有的手机号+密码登录保持不变。

## 新增接口

**发送验证码（用途新增 2=登录）**

原有接口 `POST /api/auth/send-code`，`purpose` 字段新增可选值 `2`（登录）。请求示例：
```json
{
    "email": "user@example.com",
    "purpose": 2
}
```
purpose 取值：0=注册、1=找回密码、2=登录

**邮箱验证码登录**

```
POST /api/auth/login/email
Content-Type: application/json

请求体：
{
    "email": "string",   // 邮箱，必填，需合法邮箱格式
    "code": "string"     // 6位数字验证码，必填
}

成功响应（与手机号登录一致）：
{
    "code": 1,
    "msg": "成功",
    "data": {
        "token": "string",
        "user": {
            "id": 1,
            "phone": "138xxxx",
            "email": "xxx@qq.com",
            "role": 0        // 0=普通用户 1=管理员
        }
    }
}
```

## 新增错误码

无新增错误码，复用已有错误码：

| 错误码 | 场景 |
|--------|------|
| 1004 | 验证码错误或已过期（同一验证码不可重复使用） |
| 1008 | 该邮箱未注册 |
| 1002 | 账号已被禁用 |
| 400 | 参数校验失败（邮箱格式不正确、验证码不能为空等） |

## 前端适配要点

1. **C 端登录页**：新增"邮箱验证码登录"Tab/入口
2. **发送验证码时**：登录场景传 `purpose: 2`
3. **登录接口**：`POST /api/auth/login/email`
4. **Token 使用方式不变**：响应中的 `token` 放在 `Authorization: Bearer {token}` 头
5. **验证码为一次性消费**：登录成功后该验证码即失效
