# B端管理员登录接口改动说明

日期：2026-08-03

## 概述

将原来 B/C 端共用的登录接口拆分，新增 B 端专属管理员登录接口。USER 角色用户无法登录 B 端。

## 接口变更

### 新增接口

**B端管理员登录**

```
POST /api/admin/auth/login
Content-Type: application/json

请求体：
{
    "phone": "string",    // 手机号，必填
    "password": "string"  // 密码，必填
}

成功响应：
{
    "code": 1,
    "msg": "成功",
    "data": {
        "token": "string",                    // 会话令牌，后续请求放 Authorization 头
        "user": {
            "id": 1,
            "phone": "138xxxx",
            "email": "xxx@qq.com",
            "role": 1                         // 1=管理员
        }
    }
}

失败响应（非管理员账号）：
{
    "code": 1009,
    "msg": "该账号无管理员权限"
}
```

### 原有接口行为变更

**C端用户登录（不变更路径，但仅用于 C 端）**

```
POST /api/auth/login
```

原有逻辑不变，但不应用于 B 端登录。C 端 USER 和 ADMIN 均可登录成功。

## 新增错误码

| 错误码 | 说明 |
|--------|------|
| 1009 | 该账号无管理员权限 |

## 前端适配要点

1. **B 端登录页**：将登录接口从 `POST /api/auth/login` 改为 `POST /api/admin/auth/login`
2. **Token 使用方式不变**：响应中的 `token` 字段放在请求头 `Authorization: Bearer {token}` 中使用
3. **错误处理**：新增 1009 错误码，提示用户"该账号无管理员权限"
4. **C 端登录不变**：C 端继续使用 `POST /api/auth/login`

---

# DTO 参数格式校验

日期：2026-08-03

## 概述

给登录、注册、找回密码接口的 DTO 增加了手机号和邮箱的格式校验，后端会在参数校验阶段直接拦截非法格式。

## 校验规则

| DTO | 字段 | 新增注解 | 规则 |
|-----|------|----------|------|
| LoginDTO | phone | `@Pattern` | `^1[3-9]\d{9}$` 11位手机号 |
| RegisterDTO | phone | `@Pattern` | `^1[3-9]\d{9}$` 11位手机号 |
| RegisterDTO | email | `@Email` | 标准邮箱格式 |
| ResetPasswordDTO | email | `@Email` | 标准邮箱格式 |

## 前端适配要点

- 前端已有格式校验的不受影响，加上后是双重保障
- 不合法格式会返回 `code: 400`，`msg` 为具体的校验消息（如"手机号格式不正确"、"邮箱格式不正确"）

---

# C端邮箱验证码登录

日期：2026-08-03

## 概述

C 端新增邮箱验证码登录方式，原有的手机号+密码登录保持不变。

## 新增接口

**发送验证码（用途新增 2=登录）**

原有接口 `POST /api/auth/send-code`，`purpose` 字段新增可选值 `2`（登录）。请求示例：
```json
{
    "email": "user@example.com",
    "purpose": 2
}
```
purpose 取值：0=注册、1=找回密码、2=登录

**邮箱验证码登录**

```
POST /api/auth/login/email
Content-Type: application/json

请求体：
{
    "email": "string",   // 邮箱，必填，需合法邮箱格式
    "code": "string"     // 6位数字验证码，必填
}

成功响应（与手机号登录一致）：
{
    "code": 1,
    "msg": "成功",
    "data": {
        "token": "string",
        "user": {
            "id": 1,
            "phone": "138xxxx",
            "email": "xxx@qq.com",
            "role": 0        // 0=普通用户 1=管理员
        }
    }
}
```

## 新增错误码

无新增错误码，复用已有错误码：

| 错误码 | 场景 |
|--------|------|
| 1004 | 验证码错误或已过期（同一验证码不可重复使用） |
| 1008 | 该邮箱未注册 |
| 1002 | 账号已被禁用 |
| 400 | 参数校验失败（邮箱格式不正确、验证码不能为空等） |

## 前端适配要点

1. **C 端登录页**：新增"邮箱验证码登录"Tab/入口
2. **发送验证码时**：登录场景传 `purpose: 2`
3. **登录接口**：`POST /api/auth/login/email`
4. **Token 使用方式不变**：响应中的 `token` 放在 `Authorization: Bearer {token}` 头
5. **验证码为一次性消费**：登录成功后该验证码即失效

---

# 影片状态变更逻辑修正

日期：2026-08-03

## 概述

修正两个问题：
1. 下架保护只拦截热映中影片，待映影片有关联场次也能下架
2. 上架后状态由管理员手动选择，可能选错

## 改动内容

| 改动点 | 变更前 | 变更后 |
|--------|--------|--------|
| 下架保护 | 仅 NOW_SHOWING → OFFLINE 时检查在售场次 | 只要目标状态是 OFFLINE 就检查在售场次，COMING_SOON 也不例外 |
| 上架判定 | 管理员手动选 NOW_SHOWING 或 COMING_SOON | 系统根据上映日期自动判定：releaseDate > 今天 → COMING_SOON，releaseDate ≤ 今天 → NOW_SHOWING |

## 前端适配要点

- 上架时前端仍可传 status，但服务端会**忽略**，确保不会选错
- 前端可在上架操作前根据 releaseDate 预判展示会变成哪个状态

---

# 影片编辑支持修改上映状态

日期：2026-08-03

## 概述

`MovieUpdateDTO` 增加 `status` 字段，编辑时可同时修改状态和上映日期，系统自动校验两者的逻辑关系。

## 改动内容

| 文件 | 改动 |
|------|------|
| `MovieUpdateDTO.java` | 新增 `status` 字段 |
| `MovieServiceImpl.java` | `validateMovieStatusAndDate` 增加 `isCreate` 参数；`updateMovie` 增加 status 处理 + 最终校验 |
| `MovieController.java` | 编辑接口注释更新 |

## 编辑时的校验规则

| 场景 | 结果 |
|------|------|
| 改 releaseDate + 不改 status | 用现有 status 校验新 releaseDate |
| 改 status + 不改 releaseDate | 用现有 releaseDate 校验新 status |
| 同时改 status 和 releaseDate | 校验最终组合 |
| 任意状态 → OFFLINE | 检查在售场次，有则拒绝（2002） |
| OFFLINE → 非 OFFLINE（上架） | 忽略传参，根据 releaseDate 自动判定 |

## 前端适配要点

- 编辑接口 `PUT /api/admin/movies/{id}` 现在支持 `status` 字段（可选）
- status 取值：`NOW_SHOWING`、`COMING_SOON`、`OFFLINE`
- 编辑和下架接口仍保留独立接口，单独改状态可继续用 `PUT /api/admin/movies/{id}/status`

---

# CinemaVO 注释完善 + 影厅/场次统计

日期：2026-08-03

## 概述

1. `CinemaVO` 所有字段补了 JavaDoc 注释
2. `toVO()` 中 `hallCount` 和 `showtimeCount` 从死值 0 改为真实统计
3. `hallTypes` 逻辑合并到 `toVO()`，删除冗余的 `getHallTypes()` 方法

## 影响接口

B 端和 C 端所有返回 `CinemaVO` 的接口均受影响：`hallCount` 和 `showtimeCount` 从 0 变为实际数值，`hallTypes` 在所有接口中都会返回。

---

# 影厅列表改为分页

日期：2026-08-03

## 概述

影厅列表从返回全量数组改为分页结构，支持关键词搜索。

## 接口变更

**变更前**
```
GET /api/admin/cinemas/{cinemaId}/halls
返回: { code: 1, data: [ { ...hallVO } ] }
```

**变更后**
```
GET /api/admin/cinemas/{cinemaId}/halls?page=1&size=20&keyword=IMAX
返回: { code: 1, data: { total: 50, page: 1, size: 20, records: [ ...hallVO ] } }
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认 1 |
| size | int | 否 | 每页条数，默认 20 |
| keyword | string | 否 | 模糊搜索影厅名称 |

## 前端适配要点

- 返回值结构从数组变为 `{ total, page, size, records }` 对象
- 需传分页参数
